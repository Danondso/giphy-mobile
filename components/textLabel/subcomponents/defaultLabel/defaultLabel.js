import React, { PureComponent } from 'react';
import { Text } from 'react-native';

export default class DefaultLabel extends PureComponent {
    render() {
        return (<Text>Hello!</Text>);
    }
}